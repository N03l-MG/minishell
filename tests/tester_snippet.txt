# ifndef TEST
#  define TEST false
# endif

if (TEST)
{
	if (isatty(fileno(stdin)))
		prompt = readline("");
	else
	{
		char  *line;
		line = get_next_line(fileno(stdin));
		if (line == NULL)
			break;
		prompt = ft_strtrim(line, "\n");
		free(line);
	}
}
else